{
  "name": "teams-pip-extension",
  "version": "1.0.0",
  "description": "Floating meeting controls for Microsoft Teams",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "jsdom": "^24.0.0",
    "prettier": "^3.2.0"
  }
}
